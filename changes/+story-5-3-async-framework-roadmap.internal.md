Record the Phase 3 expansion design intent for async and framework integrations without shipping any Phase 3 code. Adds `docs/roadmap.md` (Phase 1 MVP shipped · Phase 2 long-running support, with auto-config-refresh shipped in Story 5.2 · Phase 3 async API + framework helpers + observability, planned and gated) and `docs/async.md` (the sync–async coexistence model, a module-by-module forward-compatibility audit confirming no MVP module needs a rewrite to enable async, public method-name reservations, the framework-integration plan, and the open Phase-3 sign-off questions). The sync-first MVP API is unchanged and the async/framework surfaces remain unimplemented pending explicit Phase 3 sign-off.
