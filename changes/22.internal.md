Add publishable per-SDK documentation pages under `docs/site/` that mirror the JavaScript and PHP Full Stack SDK slug structure on docs.developers.convert.com (`python-quickstart`, `python-installation`, `python-initialization`, `python-configuration`, `python-code-examples`, `python-return-types`, `python-segments-manager`, `releases-python-sdk`). The existing topic guides under `docs/` remain the deeper in-repo reference.
