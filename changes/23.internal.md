Move Python-specific topic guides from in-repo `docs/` to the [GitHub wiki](https://github.com/convertcom/python-sdk/wiki) so the Python SDK matches the JS and PHP SDKs' documentation workflow. The wiki is the new canonical source for Python-specific content; cross-SDK shared concepts (bucketing algorithm, rule evaluation, data model, etc.) live in the wiki as a one-time copy of `backend/public/js/tracking/full-stack-docs/` and will become CI-synced once `convertcom/python-sdk` is added to `backend/.github/workflows/sync-to-wikis.yml`. The previously published `docs/site/` stubs (the precursor to the wiki approach, never merged to `main`) are removed in the same change. `README.md` and `tests/test_examples.py` are retargeted to the wiki page names; no test logic changes — only the section headers and docstrings that anchored snippet coverage to `docs/*.md` filenames.
