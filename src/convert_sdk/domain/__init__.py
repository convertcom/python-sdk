"""Internal domain layer for the Convert Python SDK.

Holds immutable in-memory representations of loaded config. Nothing here is part
of the public import boundary — the public surface is defined in
``convert_sdk.__init__``.
"""
