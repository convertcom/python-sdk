"""Placeholder for the SDK core entry point.

Story 1.1 freezes the public import boundary only. Real initialization and
configuration loading land in Story 1.2; this class is an intentional
placeholder so later stories can implement behavior without renaming the
public surface.
"""


class Core:
    """Public entry point placeholder for the Convert Python SDK.

    This class is deliberately empty in Story 1.1. It exists to freeze the
    ``from convert_sdk import Core`` import boundary.
    """
