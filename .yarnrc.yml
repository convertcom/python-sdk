nodeLinker: node-modules

# Supply-chain minimum release age: refuse to install any dependency version
# published less than 3 days ago (smash-and-grab malware is usually yanked
# within hours). Matches the gate in convertcom/javascript-sdk and
# convertcom/php-sdk. No npmPreapprovedPackages entry here: this project has no
# internal npm dependencies (the SDK ships via PyPI, and the only Node packages
# are release tooling), so nothing in our own release chain can stall on a
# freshly-published package.
npmMinimalAgeGate: 4320
